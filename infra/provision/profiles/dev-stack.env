# Dev stack profile — full compose: postgres + data-api + collector + worker
PROVIDER=hetzner
SERVER_TYPE=cpx31
LOCATION=ash
IMAGE=ubuntu-24.04
SSH_KEY_NAME=sessionhelper
SETUP_SCRIPT=setup/dev-stack.sh
CACHE_DIRS="docker-images configs models"
LABELS="project=sessionhelper,role=dev-stack"
