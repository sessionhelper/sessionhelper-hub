# Worker profile — runs chronicle-worker + data pipeline (CPU only)
PROVIDER=hetzner
SERVER_TYPE=cpx21
LOCATION=ash
IMAGE=ubuntu-24.04
SSH_KEY_NAME=sessionhelper
SETUP_SCRIPT=setup/worker.sh
CACHE_DIRS="docker-images configs models"
LABELS="project=sessionhelper,role=worker"
